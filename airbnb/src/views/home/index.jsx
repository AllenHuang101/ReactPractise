import React, { memo, useEffect, useState } from 'react';
import hyRequest from '@/services';

const Home = memo(() => {
  return (
    <div>
      <h2>Home Page</h2>
    </div>
  );
});

export default Home;
